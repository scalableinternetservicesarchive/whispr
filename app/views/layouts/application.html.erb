<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Whispr</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag asset_path('favicon.png') %>
  </head>

  <div class="cover-container d-flex p-3 h-100 w-100 mx-auto flex-column overflow-auto" style="max-width: 100% !important; max-height: 100vh !important; height: 100vh !important; width: 100vw !important; margin-top: 0.5%; margin-bottom: 0%; margin-left: 2.0% !important; margin-right: 2.0% !important">
    <header class="masthead mb-auto" style="margin: 1% !important">
      <div class="inner">
        <nav class="nav nav-masthead justify-content-center">
          <%= nav_link 'Home', root_path %>
          <%= nav_link 'About', about_path %>
          <% if ! current_user %>
            <%= nav_link "Sign In", sign_in_path %>
          <% else %>
            <%= nav_link "My Profile", [profile_path, profile_medical_path, profile_providers_path, patient_path] %>
            <%= nav_link "Sign Out", destroy_user_session_path, nil, "delete" %>
          <% end %>
        </nav>
      </div>
    </header>
    <body class="overflow-hidden">
      <%= stylesheet_link_tag "cover" %>
      <%= yield %>
    </body>
  </div>
</html>
